import os
import httpx
from openai import OpenAI
from crewai import Agent, Task, Crew
from rag_tool import ChromaRAGTool
from memory import add_to_memory, retrieve_memory
from dotenv import load_dotenv

load_dotenv()

# Создаём клиент с таймаутом 120 секунд
timeout_config = httpx.Timeout(120.0, connect=60.0)
http_client = httpx.Client(
    proxy=os.getenv("PROXY_URL"),
    timeout=timeout_config
) if os.getenv("PROXY_URL") else None

direct_llm = OpenAI(
    api_key=os.getenv("OPENAI_API_KEY"),
    http_client=http_client,
    timeout=120.0  # запасной таймаут для OpenAI
)

rag_tool = ChromaRAGTool()

# Субагенты
researcher = Agent(
    role="AI Researcher",
    goal="ОБЯЗАТЕЛЬНО найти релевантные бизнес-кейсы по внедрению ИИ, используя инструмент `Business Cases Search`. Если пользователь спрашивает про кейсы, ты должен вызвать инструмент и предоставить найденные документы.",
    backstory="""Вы — исследователь, который всегда начинает с поиска конкретных примеров из базы знаний (ChromaDB). 
    Если запрос пользователя содержит слова «кейс», «пример», «успешное внедрение», «агент», «автоматизация», вы ОБЯЗАНЫ выполнить поиск через инструмент.
    Вы не можете отвечать, не проверив базу. Ваш ответ должен включать найденные кейсы с их названиями, категориями, результатами и инструментами.
    
❗ **Формат ответа**: используйте Telegram-разметку:
- **жирный** для ключевых терминов
- __курсив__ для акцентов- ~~зачёркнутый~~ для устаревшей информации
- `моноширинный` для названий инструментов, кода, ссылок

Не используйте символы # для заголовков. Вместо этого делайте заголовки **жирным шрифтом** и отделяйте разделы пустыми строками.
""",
    tools=[rag_tool],
    llm="gpt-5.4-mini",
    reasoning_effort="medium",
    verbose=False
)

analyst = Agent(
    role="AI Consultant",
    goal="На основе найденных исследователем кейсов предложить конкретные, практичные решения. Если есть кейсы — перечислить их с деталями (название, результат, инструменты). Не придумывать кейсы, которых нет в поиске.",
    backstory="""Вы — консультант, который опирается исключительно на фактические данные, найденные Researcher.
    Если в контексте есть кейсы, вы обязаны перечислить их так:
    - Название кейса
    - Краткое описание
    - Достигнутые результаты (цифры, проценты)
    - Использованные инструменты
    Если кейсов нет, честно сказать об этом и дать общие рекомендации, но не выдавать выдуманные примеры.
    Не используйте общие фразы «есть много кейсов» — давайте конкретные названия.
    
❗ **Формат ответа**: используйте Telegram-разметку:
- **жирный** для ключевых выводов и названий кейсов
- __курсив__ для дополнительных пояснений
- ~~зачёркнутый~~ для нерекомендуемых подходов
- `моноширинный` для инструментов, технологий, цифр

Избегайте решёток (#). Заголовки делайте **жирным шрифтом**.
""",
    tools=[rag_tool],
    llm="gpt-5.4-mini",
    reasoning_effort="medium",
    verbose=False
)

critic = Agent(
    role="AI Critic",
    goal="Критически оценить предложенные кейсы и рекомендации: проверить реалистичность цифр, наличие доказательств, скрытые риски. Если кейсы не подкреплены данными, указать на это.",
    backstory="""Вы — критик, который проверяет, действительно ли предложенные консультантом кейсы являются доказательными.
    Вы задаёте вопросы:
    - Есть ли конкретные метрики?
    - Указан ли источник?
    - Это production или pilot?
    - Какие скрытые затраты?
    Вы не принимаете общие утверждения без цифр. Ваша задача — защитить бизнес от необоснованного оптимизма.

❗ **Формат ответа**: используйте Telegram-разметку:
- **жирный** для ключевых рисков и вердиктов
- __курсив__ для объяснений
- ~~зачёркнутый~~ для ошибочных утверждений
- `моноширинный` для точных терминов, метрик

Не используйте #. Оформляйте критические замечания чётко и структурированно.
""",
    llm="gpt-5.4-mini",
    reasoning_effort="medium",
    verbose=False
)

def run_business_crew(query: str) -> str:
    task_research = Task(
        description=f"""
        Клиент спрашивает: {query}

        **Важное указание:** если запрос содержит слова «кейс», «пример», «успешное внедрение», «агент» — ты ОБЯЗАН использовать инструмент `Business Cases Search`.
        Найди минимум 3 релевантных кейса. Для каждого укажи:
        - Название
        - Категория
        - Как реализовано
        - Результаты (метрики, если есть)
        - Инструменты

        Если ничего не найдено, сообщи об этом честно.
        """,
        expected_output="Список найденных кейсов с деталями или сообщение об отсутствии.",
        agent=researcher
    )
    task_analysis = Task(
        description=f"""
        КЛИЕНТ СПРАШИВАЕТ: {query}

        Ты получил от Researcher список найденных кейсов (в поле context).
        Твоя задача: **обязательно перечисли эти кейсы пользователю в читаемом формате**.

        Для каждого кейса укажи:
        - Название
        - Категория
        - Краткое описание (Use Case)
        - Результаты (если есть)
        - Инструменты

        Если Researcher не предоставил ни одного кейса, честно скажи об этом.
        НЕ ПРИДУМЫВАЙ кейсы, которых нет в списке.
        ПОСЛЕ ПЕРЕЧИСЛЕНИЯ кейсов можешь добавить общие рекомендации.
        """,
        expected_output="Список конкретных бизнес-кейсов с деталями (название, категория, результаты, инструменты).",
        agent=analyst,
        context=[task_research]
    )
    task_critique = Task(
        description=f"""
        Проанализируй ответ, который подготовил Analyst.
        Проверь, содержит ли он **конкретные кейсы с названиями, результатами и инструментами**.
        Если Analyst перечислил кейсы — оцени их реалистичность, наличие цифр, источников, скрытые риски.
        Если Analyst НЕ перечислил кейсы (дал общие слова) — укажи это как главный недостаток.

        Твой ответ должен быть структурой:
        1. Наличие кейсов (есть/нет)
        2. Если есть — краткая критика по каждому (реалистичность, доказательность)
        3. Если нет — требование дополнить ответ реальными примерами.
        """,
        expected_output="Критический разбор с акцентом на наличие или отсутствие конкретных кейсов.",
        agent=critic,
        context=[task_analysis]
    )
    crew = Crew(agents=[researcher, analyst, critic], tasks=[task_research, task_analysis, task_critique], verbose=True)
    result = crew.kickoff()
    return str(result)

def direct_chat(user_message: str, history_context: str) -> str:
    system_prompt = f"""
Ты — дружелюбный AI-помощник. У тебя есть возможность подключать трёх субагентов:  
- **Исследователь** (ищет инструменты и кейсы)  
- **Консультант** (предлагает решения)  
- **Критик** (выявляет риски)

Но в текущем режиме «Простой чат» ты отвечаешь самостоятельно, без них.
Если пользователь спросит про агентов, объясни, что они используются в режиме «Бизнес-консультация» (выбери его в меню).

Вот предыдущие обсуждения с пользователем (если есть):
{history_context}

Теперь пользователь пишет: {user_message}
Ответь кратко и по делу, сохраняя тон беседы.
"""
    response = direct_llm.chat.completions.create(
        model="gpt-5.4-mini",
        messages=[{"role": "user", "content": system_prompt}],
        max_completion_tokens=1000
    )
    return response.choices[0].message.content

def should_use_business_crew(message: str) -> bool:
    keywords = ["ии", "ai", "чат-бот", "нейросеть", "автоматизация", "бизнес-процесс", "внедрение", "кейс", "оптимизация", "рентабельность", "риски"]
    return any(kw in message.lower() for kw in keywords)

def orchestrate(user_id: str, user_message: str, mode: str = "auto") -> str:
    """
    mode: 'chat' - только простой чат
          'consult' - только бизнес-консультация (субагенты)
          'auto' - автоматический выбор по ключевым словам
    """
    memory_context = retrieve_memory(user_id, user_message, n_results=3)

    if mode == "chat":
        response = direct_chat(user_message, memory_context)
    elif mode == "consult":
        response = run_business_crew(user_message)
    else:  # auto
        if should_use_business_crew(user_message):
            response = run_business_crew(user_message)
        else:
            response = direct_chat(user_message, memory_context)

    add_to_memory(user_id, user_message, response)
    return response
